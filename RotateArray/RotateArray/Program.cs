using System;
class Program
{
    static void Main()
    {
        int[] arrayToRotate = { 7, 1, 5, 3, 6, 4, 1, 2, 3, 4 };
        Console.WriteLine("arrayToRotate: " + string.Join(", ", arrayToRotate));

        int output = Rotate(arrayToRotate);

        Console.WriteLine("Output: " + output);



    }

    public void Rotate(int[] nums, int k)
    {

        for(int i=0;i<nums.Length; i++)
        {
            //nums[]
        }

    }
}

